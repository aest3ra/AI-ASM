target: http://localhost:8888/

scope:
  include_domains:
    - localhost
  exclude_paths:
    - /logout
    - /signout

limits:
  max_pages: 120
  max_duration_sec: 420
  rate_limit_rps: 2
  max_visits_per_template: 3

auth:
  type: none

agent:
  mode: planner
  model: gpt-5-mini
  max_steps_per_page: 24
  form_data_path: testdata/forms/default.yaml
